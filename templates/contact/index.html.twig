{% extends 'base.html.twig' %}

{% block title %}Contact us{% endblock %}

{% block body %}

<div class="mt-5">
    <div class="container col-6">
        <h1>Contact</h1>
        {{ form_start(formView) }}
        {{ form_widget(formView) }}
        <button type="submit" class="btn-l">Send</button>
        {{ form_end(formView) }}
    </div>
</div>
{% endblock %}
