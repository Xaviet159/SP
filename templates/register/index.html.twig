{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}

<div class="mt-5">
    <div class="container col-6">
        <h1>Inscription</h1>
        {{ form_start(formView) }}
        {{ form_widget(formView) }}
        <button type="submit" class="btn btn-success mt-2">enoyer</button>
        {{ form_end(formView) }}
    </div>
</div>
{% endblock %}
